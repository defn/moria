version: '3.7'
services:
  vault:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.vault.rule=Host(`vault.${KITT_DOMAIN}`)"
      - "traefik.http.services.vault.loadbalancer.server.port=8200"

networks:
  default:
    external:
      name: kitt
